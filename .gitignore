node_modules/
dist/
.env
.env.local
.firebase/
*.log
.DS_Store

# 예전 Apps Script 버전 (샘플 데이터에 평문 비밀번호 포함 — git에 올리지 않음)
이전 파일/

# 아이콘 원본 (가공된 결과물은 public/icons/에 보관)
/머더미스터리 앱 아이콘.png
/앱 아이콘 배경제거.png

# 시나리오 원본 엑셀 (Firestore로 가져온 뒤에는 git에 안 올림)
*.xlsx

# "사건이 도착했습니다" 콘텐츠 원본(정답·진상 포함) — 공개 저장소라 절대 커밋하지 않음.
# 시딩은 어드민 페이지 "JSON 가져오기"로만 함.
/사건파일/

# Claude Code 내부 상태 파일
.claude/scheduled_tasks.lock
